INCLUDE_DIRECTORIES(
	${VLE_SOURCE_DIR}/src
	${GLIBMM_INCLUDE_DIRS}
	${LIBXMLPP_INCLUDE_DIRS})

LINK_DIRECTORIES(
	${GLIBMM_LIBRARY_DIRS}
	${LIBXMLPP_LIBRARY_DIRS})

ADD_EXECUTABLE(vle main.cpp OptionGroup.cpp OptionGroup.hpp)

TARGET_LINK_LIBRARIES(vle
	${GLIBMM_LIBRARIES}
	${LIBXMLPP_LIBRARIES}
	${GTHREAD_LIBRARIES}
	vlegeometry
	vleutils
	vledata
	vlevalue
	vlegraph
	vlevpz
	vledevs
	vleextension
	vlemanager
	vletranslator)

INSTALL(TARGETS vle DESTINATION ${VLE_BIN_DIR})
INSTALL(FILES vle.1 DESTINATION ${VLE_MANPAGE_PATH})
