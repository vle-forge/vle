INCLUDE_DIRECTORIES(
  ${VLE_SOURCE_DIR}/src
  ${Boost_INCLUDE_DIRS}
  ${GLADEMM_INCLUDE_DIRS}
  ${GTKMM_INCLUDE_DIRS}
  ${LIBXMLPP_INCLUDE_DIRS}
  ${GTHREAD_INCLUDE_DIRS})

LINK_DIRECTORIES(
  ${VLE_LIBRARY_DIRS}
  ${LIBXMLPP_LIBRARIES}
  ${GLADEMM_LIBRARY_DIRS}
  ${GTKMM_LIBRARY_DIRS}
  ${GTHREAD_INCLUDE_DIRS})

ADD_EXECUTABLE(gvle main.cpp)

SET_TARGET_PROPERTIES(gvle PROPERTIES ${VLE_APP_PROPERTIES})

TARGET_LINK_LIBRARIES(gvle
  ${VLE_LIBRARIES}
  ${GLADEMM_LIBRARIES}
  ${GTKMM_LIBRARIES}
  ${GTHREAD_LIBRARIES}
  ${OS_SPECIFIC_LIBRARIES}
  vlegeometry
  vleutils
  vledata
  vlevalue
  vlegraph
  vleoov
  vlevpz
  vledevs
  vleextension
  vlemanager
  vletranslator
  vlegvle)

INSTALL(TARGETS gvle DESTINATION bin)
INSTALL(FILES gvle.1 DESTINATION ${VLE_MANPAGE_PATH})
