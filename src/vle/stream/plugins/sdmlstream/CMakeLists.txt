INCLUDE_DIRECTORIES(
	${VLE_SOURCE_DIR}/src
	${GLIBMM_INCLUDE_DIRS}
	${LIBXMLPP_INCLUDE_DIRS})

LINK_DIRECTORIES(
	${GLIBMM_LIBRARY_DIRS}
	${LIBXMLPP_LIBRARY_DIR})

ADD_LIBRARY(sdmlstream MODULE SDMLStream.cpp SDMLStream.hpp)

INSTALL(TARGETS sdmlstream DESTINATION ${VLE_LIBRARY_DIRS}/stream)
