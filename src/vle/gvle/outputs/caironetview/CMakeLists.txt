INCLUDE_DIRECTORIES(
  ${VLE_BINARY_DIR}/src
  ${VLE_SOURCE_DIR}/src
  ${Boost_INCLUDE_DIRS}
  ${GLIBMM_INCLUDE_DIRS}
  ${LIBXML_INCLUDE_DIRS}
  ${GLADEMM_INCLUDE_DIRS}
  ${GTKMM_INCLUDE_DIRS}
  ${GTHREAD_INCLUDE_DIRS})

LINK_DIRECTORIES(
  ${GLIBMM_LIBRARY_DIRS}
  ${LIBXML_LIBRARY_DIR}
  ${GLADEMM_LIBRARY_DIRS}
  ${GTKMM_LIBRARY_DIRS}
  ${GTHREAD_LIBRARY_DIRS})

ADD_LIBRARY(output-caironetview MODULE CairoNetView.hpp CairoNetView.cpp)
SET_TARGET_PROPERTIES(output-caironetview PROPERTIES OUTPUT_NAME "caironetview")

TARGET_LINK_LIBRARIES(output-caironetview
  ${GLIBMM_LIBRARIES}
  ${LIBXML_LIBRARIES}
  ${CAIROMM_LIBRARIES}
  ${GLADEMM_LIBRARIES}
  ${GTKMM_LIBRARIES}
  ${GTHREAD_LIBRARIES}
  ${OS_SPECIFIC_LIBRARIES}
  vlemanager
  vleutils
  vlevalue
  vlegraph
  vlevpz
  vleoov
  vlevpz
  vledevs
  vleextension
  vlemanager
  vletranslator
  vlegvle)

INSTALL(TARGETS output-caironetview
  RUNTIME DESTINATION "${VLE_LIBRARY_DIRS}/outputs"
  LIBRARY DESTINATION "${VLE_LIBRARY_DIRS}/outputs")

INSTALL(FILES caironetview.glade DESTINATION ${VLE_SHARE_DIRS}/outputs/glade)

