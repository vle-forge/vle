INCLUDE_DIRECTORIES(
  ${VLE_BINARY_DIR}/src
  ${VLE_SOURCE_DIR}/src
  ${Boost_INCLUDE_DIRS}
  ${GLIBMM_INCLUDE_DIRS}
  ${GTKMM_INCLUDE_DIRS}
  ${LIBXML_INCLUDE_DIRS}
  ${CAIROMM_INCLUDE_DIRS})

LINK_DIRECTORIES(
  ${GLIBMM_LIBRARY_DIRS}
  ${GTKMM_LIBRARY_DIR}
  ${LIBXML_LIBRARY_DIR}
  ${CAIROMM_LIBRARY_DIRS})

ADD_LIBRARY(cairolevel SHARED CairoLevel.cpp CairoLevel.hpp)

TARGET_LINK_LIBRARIES(cairolevel
  ${GLIBMM_LIBRARIES}
  ${GTKMM_LIBRARIES}
  ${LIBXML_LIBRARIES}
  ${GTHREAD_LIBRARIES}
  ${CAIROMM_LIBRARIES}
  vlegeometry
  vleutils
  vlevalue
  vlegraph
  vleoov)

INSTALL(TARGETS cairolevel
  RUNTIME DESTINATION "${VLE_LIBRARY_DIRS}/stream"
  LIBRARY DESTINATION "${VLE_LIBRARY_DIRS}/stream")
