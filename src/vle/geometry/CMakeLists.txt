INCLUDE_DIRECTORIES(
  ${VLE_SOURCE_DIR}/src
  ${GLIBMM_INCLUDE_DIRS}
  ${LIBXMLPP_INCLUDE_DIRS})

LINK_DIRECTORIES(
  ${GLIBMM_LIBRARY_DIRS}
  ${LIBXMLPP_LIBRARY_DIR})

SET(GEOMETRY_CPP geometry.cpp)

SET(GEOMETRY_HPP Box.hpp Point.hpp Tuple.hpp Vector.hpp)

ADD_LIBRARY(vlegeometry SHARED ${GEOMETRY_HPP};${GEOMETRY_CPP})

INSTALL(TARGETS vlegeometry
  RUNTIME DESTINATION bin
  LIBRARY DESTINATION lib
  ARCHIVE DESTINATION lib)

INSTALL(FILES ${GEOMETRY_HPP} DESTINATION ${VLE_INCLUDE_DIRS}/geometry)
