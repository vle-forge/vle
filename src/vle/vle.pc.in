prefix=${VLE_INSTALL_PREFIX}
exec_prefix=${VLE_INSTALL_PREFIX}
libdir=${VLE_LIBRARY_DIR}
includedir=${VLE_INCLUDE_DIRS}

Name: vle
Description: VLE multimodelling and Simulation tools
Requires: libxml++-2.6 glibmm-2.4
Version: ${VLE_PACKAGE_VERSION}
Libs:	-L${VLE_LIBRARY_DIR} -lvledata -lvledevs -lvleextension -lvlegeometry \
	-lvlegraph -lvlemanager -lvleutils -lvlevalue -lvlevpz -lvletranslator
Cflags: -I${VLE_INCLUDE_DIR}
