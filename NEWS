# VLE 2.1

## Changes from VLE 2.0

### cmake

- Bump to modern CMake (minimum version 3.5).

- Remove qmake script.

### gvle

- Remove the copy of QCustomplot in GVLE. Please install libqcustomplot-dev to
  enable the build of the DefaultSimSubpanel plugin.

### vle

- Remove the temporary files or directories: Mainly on Windows, the temporary
  directory must be cleaned (remove all temporary files or directories) to
  exclude bug with already downloaded file etc. (Closes: #364).

